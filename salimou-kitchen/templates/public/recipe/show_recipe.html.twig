{% extends "public/base.html.twig" %}

{% block title %}{{ recipe.title }} | Les recettes de Salimou{% endblock %}

{% block body %}

    <main>

        <article>
            <p>{{ recipe.title }}</p>

            <img  src="{{ asset('assets/upload/' ~ recipe.image) }}" />

            <p>{{ recipe.instructions }}</p>

            <p>Catégorie : {{ recipe.category.title }}</p>
        </article>

    </main>

{% endblock %}